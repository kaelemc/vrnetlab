hostname <hostname>
!
no aaa new-model
!
ip cef
!
no ip domain lookup
!
username admin privilege 15 password admin
!
vrf definition clab-mgmt
 address-family ipv4
 !
!
interface Ethernet0/0
 vrf forwarding clab-mgmt
 ip address <ip_address> 255.255.255.0
 <eth0_mac>
 no shutdown
!
<interfaces>
!
ip forward-protocol nd
!
ip route vrf clab-mgmt 0.0.0.0 0.0.0.0 Ethernet0/0 <ip_gateway>
ip domain name lan
ip ssh version 2
crypto key generate rsa modulus 2048
!
line vty 0 4
 login local
 transport input ssh
!
end